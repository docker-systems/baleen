web:
  build: .
  links:
   - db
  ports:
   - "8000:8000"
   - "9001:9001"
  environment:
   POSTGRES_PASSWORD:
   POSTGRES_USER:
  volumes_from:
    - "baleen-config"
  #volumes:
  #- /usr/local/d8o/baleen:/usr/local/baleen
db:
  image: postgres:9.4
  environment:
   POSTGRES_PASSWORD:
   POSTGRES_USER:
